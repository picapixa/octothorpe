<nav class="bg-gray-900 z-10 lg:py-4 lg:px-8{{#if has_featured_image}} bg-opacity-50{{/if}}">
  <div class="container flex items-stretch h-16 mx-auto my-0">
    <div class="px-4 py-5 inline-flex items-center">
      <span class="">
        <span class="font-semibold">raffy</span><span class="font-light">alcoriza</span>&nbsp;/&nbsp;<a href="{{ @site.url }}" class="no-underline text-gray-100 hover:text-white"><strong>{{ @site.title }}</strong></a>
      </span>
    </div>
  </div>
</nav>
